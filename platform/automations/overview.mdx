---
title: "Automations"
description: "Act on qualifying findings automatically: remediate them as they are reported and notify your team."
---

Automations act on findings for you. Configure which findings qualify, and when a matching finding is reported Hacktron fires the automation: its **Autofix** action starts a [remediation](/platform/automations/remediation) run that fixes the finding, and its notifiers alert your team over Slack or email.

Automations live under **Automations** in the dashboard sidebar. Organization **Admins** create and manage automations; other members can view them.

## Create an automation

<Steps>
  <Step title="Create">
    On the **Automations** page, select **New automation** and give it a name.

    <Frame>
      ![Automations 1](/images/automations-1.png)
    </Frame>
  </Step>
  <Step title="Pick environments">
    On the **Environments** tab, add the repositories and [applications](/platform/context/applications) the automation covers. Each entry can be limited to specific branches (up to two); leaving the branch selection empty covers all branches.

    <Frame>
      ![Screenshot 2026 07 09 At 1 19 23 PM](/images/Screenshot-2026-07-09-at-1.19.23-PM.png)
    </Frame>
  </Step>
  <Step title="Configure settings">
    On the **Settings** tab, configure:

    - **Triggers**: the event that fires the automation, for example a PR scan finding, plus rules that narrow it down: the severity levels that qualify, and scope rules that limit it to specific repositories or applications.
    - **Actions**: what the automation does. **Autofix** starts a [remediation](/platform/automations/remediation) run for the matching finding.
    - **Notifiers**: where to announce a fired run, a Slack channel, email recipients from your organization's members, or both.

    <Frame>
      ![Screenshot 2026 07 09 At 1 22 05 PM](/images/Screenshot-2026-07-09-at-1.22.05-PM.png)
    </Frame>
  </Step>
  <Step title="Save">
    Select **Save**. An automation with environments, a trigger, and an action is enabled automatically; you can toggle it off at any time from the list or detail page.
  </Step>
</Steps>

<Note>
  An automation needs at least one environment, one trigger, and one action to run. Incomplete automations show an **Incomplete** badge and cannot be enabled. A save that removes required configuration disables the automation until it is complete again.
</Note>

## When an automation fires

When a new finding is reported, an enabled automation fires if the finding matches its trigger platform, severity rules, scope rules, and one of its environments (including any branch limits). Firing starts a remediation run, which consumes [automation credits](/platform/automations/credits), and sends the configured notifications.

Each finding is remediated at most once: a finding that already has a remediation run does not fire automations again.

<Info>
  A repository with a `remediation` block in its [`.hacktron/config.yaml`](/code-review/config#auto-remediate-findings) follows that block exclusively, including `auto: false`, which turns auto-remediation off for that repository. Organization automations apply only to repositories without a `remediation` block.
</Info>

### Notifications

- **Slack**: Hacktron posts a message to the configured channel linking the finding. The Hacktron bot joins public channels automatically; for a private channel, invite the bot to the channel first.
- **Email**: Hacktron emails the configured recipients. Recipients are chosen from your organization's members.

Notifications are best-effort: a notification failure never blocks the remediation run itself.

## Run history

The **Run history** tab lists every run the automation has fired, with its status, the environment it matched, and the credits it consumed. Runs matched through an application are attributed to that application.

Run statuses mirror [remediation outcomes](/platform/automations/remediation#outcomes): **Running**, **Success**, **No fix needed**, **False positive**, and **Failed**. You can filter the history by status and search it.

## Related docs

<Columns cols={2}>
  <Card title="Remediation" icon="wrench" href="/platform/automations/remediation">
    How a remediation run works and where the fix lands.
  </Card>

  <Card title="Automation credits" icon="coins" href="/platform/automations/credits">
    How runs are billed and how to keep a balance with auto top-up.
  </Card>
</Columns>